---
title: "`r params$report_title`"
output: 
  html_document:
    toc: true
    toc_float: true
    theme: united
    code_folding: hide
params:
  report_title: "Parsing CDS sequences from a genome"
  report_author: "Tamas Szabo"
  report_time: !r format(Sys.Date(), format="%d/%m/%Y")
  species: "human"
  data_folder: "../data"
---

## Setup

```{r message = FALSE, warning = FALSE}
# Import tools to be used

library(dplyr)
library(GenomicFeatures)

select <- dplyr::select
filter <- dplyr::filter
```
```{r}
BiocManager::install('TxDb.Dmelanogaster.UCSC.dm6.ensGene')
BiocManager::install('TxDb.Drerio.UCSC.danRer10.refGene')
```


```{r}
library(EnsDb.Hsapiens.v86)
library(EnsDb.Mmusculus.v79)
library(BSgenome.Drerio.UCSC.danRer10)
library(BSgenome.Dmelanogaster.UCSC.dm6)
library(TxDb.Drerio.UCSC.danRer10.refGene)
library(TxDb.Dmelanogaster.UCSC.dm6.ensGene)

hsdb <- EnsDb.Hsapiens.v86
mmdb <- EnsDb.Mmusculus.v79
drdb <- BSgenome.Drerio.UCSC.danRer10
dmdb <- BSgenome.Dmelanogaster.UCSC.dm6

hs_dna <- ensembldb:::getGenomeTwoBitFile(hsdb)
mm_dna <- ensembldb:::getGenomeTwoBitFile(mmdb)
```


```{r message = FALSE, warning = FALSE}
cdsByTx <- cdsBy(mmdb, "tx")
```

```{r}
dna <- extractTranscriptSeqs(mmdb, cdsByTx)
```

```{r}
drosi_cds <- cdsBy(TxDb.Dmelanogaster.UCSC.dm6.ensGene, "tx")
```

```{r}
drosi_dna_seqs  <- extractTranscriptSeqs(BSgenome.Dmelanogaster.UCSC.dm6, drosi_cds)
drosi_rna_seqs  <- reverseComplement(drosi_dna_seqs)
drosi_rna_seqs  <- RNAStringSet(drosi_rna_seqs)
```

```{r}

```

